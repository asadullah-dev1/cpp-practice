#include <iostream>
using namespace std;
int main(){
    int m,n,p,amount,result;
    cout<<"Enter the value of m,n,p and amount "<<endl;
    cin>> m >> n >> p >> amount;
        result=(m+n)/(p+amount);
        cout<<" the result will be " << result << endl;
        return 0;
}